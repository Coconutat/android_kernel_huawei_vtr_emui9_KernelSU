config HW_LOGGER
	bool "Huawei log driver"
	default n
	---help---
	  This adds support for system-wide logging using two log buffers.
	  Plus adds reboot watchdog for triggering logcat and xlogcat flush.

	  These are:

	      1: hwlog_exception
	      2: hwlog_jank
